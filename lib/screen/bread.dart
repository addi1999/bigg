import 'package:flutter/material.dart';

class breadcreen extends StatelessWidget {
  const breadcreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("its breadpage"),
    );
  }
}