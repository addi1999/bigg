import 'package:flutter/material.dart';

class Carouselscreen extends StatelessWidget {
  const Carouselscreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("its carouselpage"),
    );
  }
}