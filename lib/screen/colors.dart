import 'package:flutter/material.dart';

class olorsscreen extends StatelessWidget {
  const olorsscreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("its colorpage"),
    );
  }
}
