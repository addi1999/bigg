import 'package:flutter/material.dart';

class Typography1 extends StatelessWidget {
  const Typography1();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("its Typographypage"),
    );
  }
}
