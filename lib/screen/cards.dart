import 'package:flutter/material.dart';

class cardsscreen extends StatelessWidget {
  const cardsscreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("its cardspage"),
    );
  }
}