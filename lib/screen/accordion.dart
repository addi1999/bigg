import 'package:flutter/material.dart';

class Accordion extends StatelessWidget {
  const Accordion();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("its accordionpage"),
    );
  }
}